{
  "name": "rconman-web",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i input.css -o static/app.css",
    "watch": "tailwindcss -i input.css -o static/app.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0"
  },
  "dependencies": {
    "daisyui": "^4.0.0"
  }
}
